<03.05>
<python>
# 마크다운 : 문양(태그)을 사용한 텍스트 입력방식
## 헤헤헿
### 헤헤헤헿

=> # 스페이스바 : 마크다운
=> #은 h1~h6
=> 공백을 두번쓰면 줄 개행

* 문자열과 숫자 더하기는 불가능
 => 자바, 자바스크립트는 다 가능

* 문자열 + 변수 : print(f'내가 사는 곳은 {addr}입니다')

* int( ) : 문자 -> 숫자
  str( ) : 숫자 -> 문자

* 실무에 가면 디비에 자료가 엄청나게 많은데 HAVING에 조건을 걸고, SELECT에서 집계함수를 걸어버리면 시간이 너무 오래 걸릴뿐더러 잘못하면 서버 터질 수 있어서 WHERE로 먼저 조건에 맞는 데이터들을 고르고 SELECT할 수 있도록 하는 것이 안전함. 